// Locks anchor point to bottom of object //

var boxTop = thisLayer.sourceRectAtTime().top; // Position of top of textbox //
var boxHeight = thisLayer.sourceRectAtTime().height; // Height of textbox //

// Offset anchor down from boxTop using half the height of the box //

[value[0], boxTop + boxHeight ];﻿
